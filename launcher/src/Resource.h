#pragma once

// Application icon, shared by the launcher window class and the .exe resources
// (also picked up by Explorer, the taskbar and the .zlaunch file association,
// which points at "<exe>,0").
#define IDI_APPICON 101
